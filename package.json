{
  "name": "raw",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "reload",
    "deploy": "next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"stamp.constraint.systems\" >> out/CNAME && git add out/ && git commit -m \"Deploy to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "reload": "^3.0.4"
  }
}
